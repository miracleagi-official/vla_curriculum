# Dev Log

## 2026-04-17

### _forAI 문서 세트 정리

- forai-scaffold 기준으로 _forai 문서 구조를 점검했다.
- readme.md를 표준 가이드 형식으로 작성했다.
- plan.md를 목표/작업/결정/리스크 구조로 재작성했다.
- inventory.md, memo.md, dev_log.md를 신규 생성했다.

### 온라인 트랙 계획 정리

- 기존 초급/중급 커리큘럼 문서를 기준으로 온라인 전용 트랙 기획 방향을 다시 정리했다.
- plan.md를 목표, 범위, 우선순위, 검증 이슈, 작업 순서 기준으로 구체화했다.
- `Omniverse`와 `Isaac Sim` 중심 실습 흐름을 35강 기준 모듈 초안으로 재배치했다.

### 온라인 트랙 순서 재정렬

- 온라인 트랙의 기준 서사를 `LeRobot/SO101` 중심으로 다시 잡았다.
- `Omniverse` 독립 입문 구성을 줄이고 `Isaac Sim` 실습 안에 필요한 최소 개념만 포함하는 방향으로 수정했다.
- plan.md의 모듈 순서를 `LeRobot -> Isaac Sim 실습 -> 프로젝트` 흐름으로 전면 재배치했다.

### 온라인 트랙 초안 문서 작성

- `online_curriculum.md`를 신규 작성해 35강 구성의 온라인 시뮬레이션 트랙 초안을 추가했다.
- readme.md에 온라인 트랙 초안 링크와 위치 설명을 반영했다.
- inventory.md와 memo.md를 새 온라인 문서 기준으로 갱신했다.

### 온라인 트랙 폴더 구조 분리

- `online_track/` 디렉터리를 만들고 `modules/`, `references/` 하위 구조를 추가했다.
- 모듈 1~6용 상세 문서 골격 파일을 생성했다.
- 기존 `online_curriculum.md`는 상위 개요 문서로 남기고, 세부 설계는 `online_track/` 아래에서 확장하는 구조로 정리했다.

### 모듈 1 상세 강의안 작성

- `online_track/modules/module_01_lerobot_pipeline.md`를 실제 강의안 수준으로 확장했다.
- 모듈 목표, 차시 요약표, 1~4강의 20분 진행안, 준비 자료, 과제, 체크포인트를 추가했다.
- 설명 톤은 기술 문서보다 강의용 문체에 가깝게 조정했다.

### 온라인 트랙 차시 재배치

- 모듈 1의 억지스러운 4강 구성을 3강으로 줄이고, 수업 진행 방식 안내는 1강에 통합했다.
- 남는 1강은 모듈 4로 이동해 `미러 태스크 1차 실행과 장면 점검` 차시를 추가했다.
- `online_curriculum.md`, `online_track/readme.md`, 모듈 1~4 문서, `_forai/plan.md`의 강수와 차시 번호를 새 분배에 맞춰 정리했다.

### 온라인 트랙 제목 쉬운 표현으로 정리

- `online_curriculum.md`의 모듈 제목과 차시 제목을 일반인 기준으로 더 쉬운 표현으로 바꿨다.
- `online_track/readme.md`와 모듈 1~6 문서의 제목, 역할, 현재 포함 차시 문구를 같은 톤으로 맞췄다.
- 제품명은 유지하되 `태스크`, `평가`, `미러`, `파이프라인` 같은 표현은 제목에서 최대한 쉬운 말로 풀었다.

### 모듈 5-6 방향 재조정

- 모듈 5의 `기록/평가` 중심 구성을 `Isaac Sim` 기능을 더 써보는 응용 블록으로 바꿨다.
- 모듈 6의 `작은 프로젝트` 구성을 `Omniverse`와 `Isaac Sim` 활용 확장 이해 블록으로 바꿨다.
- `online_curriculum.md`, `online_track/readme.md`, 모듈 5~6 문서, `_forai/plan.md`를 새 방향에 맞게 수정했다.

### 데이터셋 생성 흐름 보강

- 온라인 커리큘럼에서 데이터셋 생성이 약하게 보이던 문제를 반영해 모듈 5를 다시 조정했다.
- 모듈 5를 `데이터셋 만들고 간단한 코드 써보기`로 바꾸고, 저장 항목, 폴더 구조, 기초 Python 예제, 데이터 점검 흐름을 넣었다.
- 최종 산출물과 평가 기준에도 데이터셋 이해도와 기초 코드 이해도를 반영했다.

### 문서 현황 재점검

- `forai-scaffold` 스크립트로 `_forAI` 표준 문서 세트를 재점검했고 추가 생성이나 갱신이 필요한 파일은 없음을 확인했다.
- 상위 문서 `readme.md`, `basic_curriculum.md`, `intermediate_curriculum.md`, `online_curriculum.md`, `advanced_curriculum.md`를 다시 읽고 저장소 전체가 `SO101 + LeRobot` 공통 기준 위에 정리되어 있음을 확인했다.
- `online_track/`는 상위 온라인 트랙 개요를 세부 모듈로 쪼개는 작업 공간이며, 현재는 모듈 1만 상세 강의안 수준이고 모듈 2~6과 `references/` 문서는 골격 중심 상태임을 확인했다.
